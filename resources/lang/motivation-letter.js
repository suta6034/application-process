import {
    extend,
} from '../utils/i18n';

export const strings = {
    motivationLetter: {
        created: 'Bewerbungsschreiben wurde angelegt.',
        updated: 'Bewerbungsschreiben wurde aktualisiert.',
        deleted: 'Bewerbungsschreiben wurde gelöscht.',
    },
};

extend(strings);
