import {
    extend,
} from '../utils/i18n';

export const strings = {
    message: {
        deleted: 'Nachricht wurde gelöscht.',
        sent: 'Nachricht gesendet.',
        sentFromTrash: 'Nachricht gesendet und in Posteingang verschoben.',
    },
};

extend(strings);
